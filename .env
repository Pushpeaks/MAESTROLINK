# Local environment variables for Vite (do NOT commit secrets to public repos)
# Replace with your Google OAuth client id (OAuth 2.0 client ID for Web apps)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Backend API URL used by the frontend (point to local auth server when running it)
VITE_MERN_API_URL=http://localhost:4000
